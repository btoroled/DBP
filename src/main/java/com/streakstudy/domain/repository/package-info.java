/**
 * Interfaces de repositorio (puertos de salida del dominio).
 *
 * <p>Aqui se declaran los contratos para persistencia. Las implementaciones
 * concretas (JPA, in-memory, etc.) viven en {@code infrastructure.persistence}.</p>
 */
package com.streakstudy.domain.repository;
